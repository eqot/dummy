{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "lerna run start --scope dummy-core",
    "build": "lerna run build --scope dummy-core",
    "doc:start": "cd packages/dummy-doc/website && yarn start",
    "doc:deploy": "cd packages/dummy-doc/website && yarn publish-gh-pages"
  },
  "devDependencies": {
    "lerna": "^3.13.3"
  }
}
